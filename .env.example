# Copy this file to `.env` in the project root and replace the placeholders with your EmailJS values.
# Then restart the dev server.

# Get these values from https://www.emailjs.com/
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# Example template variables used in the code: from_name, reply_to, message
# Make sure your EmailJS template expects these variables.
